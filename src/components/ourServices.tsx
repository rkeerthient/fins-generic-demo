import { TemplateProps } from "@yext/pages/*";

const OurServices = ({ document }: any) => {
  const { c_linkedInsightsArticles } = document;
  return (
    <section>
    </section>
  );
};

export default OurServices;
